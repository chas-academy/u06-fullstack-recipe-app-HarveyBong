import { RecipebyidPipe } from './recipebyid.pipe';

describe('RecipebyidPipe', () => {
  it('create an instance', () => {
    const pipe = new RecipebyidPipe();
    expect(pipe).toBeTruthy();
  });
});
